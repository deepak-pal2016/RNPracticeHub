import Colors from './colors';
import Fonts from './fonts';
import Images from './images';
import Typography from './fontSize';
import Icon from './fonticons';
const cardShadow = {
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.1,
  shadowRadius: 4,
  elevation: 3,
}

export {Fonts, Colors, Images, Typography,Icon,cardShadow};
